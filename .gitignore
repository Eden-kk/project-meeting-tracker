# Python (storage-router, transcript-ingest, voice-ingest)
.venv/
var/
__pycache__/
.pytest_cache/
*.pyc
*.pyo
.env
.env.local
.env*.local
.coverage
.ruff_cache/
*.egg-info/
build/
htmlcov/

# Voice-ingest specifics (Whisper model cache + audio scratch)
/models/
audio_scratch/

# Node / Vite (frontend)
node_modules/
dist/
playwright-report/
test-results/
coverage/

# Zoom-bot npm deps (Wave 9 — bot/ is a sibling project to src/ with its own package.json)
bot/node_modules/

# Editor / tooling / scratch
.claude/
.worktrees/
.claude/
