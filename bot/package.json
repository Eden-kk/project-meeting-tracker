{
  "name": "hermes-zoom-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Headless Chromium driver that lets Hermes join a Zoom call as a participant and stream audio chunks to the storage-router.",
  "main": "zoom-bot.js",
  "scripts": {
    "start": "node zoom-bot.js"
  },
  "dependencies": {
    "puppeteer": "^22.15.0"
  },
  "engines": {
    "node": ">=18"
  }
}
